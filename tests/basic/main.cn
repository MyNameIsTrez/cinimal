import { assert } from <assert.h>
import { i32 } from <stdint.h>
import { print } from <stdio.h>

void add(n: ptr mut i32)
	n*++

i32 main()
	i: mut i32
	i = 0

	while i < 3
		if i == 0
			print("foo")
		else
			print("bar")
		i++

	assert(i == 3)
	add(&i)
	assert(i == 4)

	return 0
