import { assert } from <assert.h>
import { i32 } from <stdint.h>
import { print } from <stdio.h>

void add(ptr mut i32 n)
	n*++

i32 main()
	mut i32 i
	i = 0

	while i < 3
		if i == 0
			print("foo")
		else
			print("bar")
		i++

	assert(i == 3)
	add(&i)
	assert(i == 4)

	return 0
